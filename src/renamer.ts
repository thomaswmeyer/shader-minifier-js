// Port of Minifier/renamer.fs -- PHASE 3 STUB (no renaming)
import type { ExportedName, Shader } from "./ast.js";
import type { Options } from "./options.js";

export function rename(_options: Options, _shaders: Shader[]): ExportedName[] {
  return [];
}
